$(document).ready(function () {    $('#group_list').load('/operator_group_subs_list/');});$('#operator_group').form({    on : 'blur',    fields:{        group_name : {            identifier : 'group_name',            rules:[{                type: 'empty',                prompt : '请输入客服组名称'            }]            }        }});function addgroup() {    var message=$("#operator_group").form('is valid');    if (message==true)    {var group_name=$.trim($('#group_name').val());    $.ajax({        type : 'POST',        url : '/add_operator_group/',        data: {'group_name':group_name},        success : function (result) {            if(result === 'True'){                alert('新增成功');                $('#group_list').load('/operator_group_subs_list/');                $('#group_name').val("")            }            else {                alert('新增失败')            }                }    })}};